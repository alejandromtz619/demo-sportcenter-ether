{
  "name": "demo-sportcenter-ether",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "install": "cd frontend && npm install --legacy-peer-deps --force"
  }
}
